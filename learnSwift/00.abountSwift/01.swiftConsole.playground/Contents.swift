import UIKit

var str = "HelloSwift, HelloPlayGround";
print(str);
var j: Int = 0;

for i in 0...3{
    j+=i;
}
/**
    위와 같이 값이 변경되거나 반복 실행 등의 작업을 하게 되면 과정을 확인할 수 있도록 빠른 확인 보이기 뷰를 볼 수 있다.
 
 
 
    * REPL
 
    스위프트는 인터프리터 언어에서 주로 사용되는 REPL(Read-Eval-Print-Loop)로도 코드를 실행할 수 있스다. 물론 스위프트는 인터프리터 언어가 아닌 컴파일 언어지만 기존의 잉ㄴ터프리터 언어의 REPL과 거의 비슷하게 사용할 수 있다. REPL은 간단한 코드를 직접입력하여 바로바로 결과를 볼 수 있는 환경과 편의를 제공한다 .
 */

